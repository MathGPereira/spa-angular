import { MesmaSenhaDirective } from './mesma-senha.directive';

describe('MesmaSenhaDirective', () => {
  it('should create an instance', () => {
    const directive = new MesmaSenhaDirective();
    expect(directive).toBeTruthy();
  });
});
